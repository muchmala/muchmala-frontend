server {
    listen <%= config.static.port %>;
    server_name <%= config.static.host %>;

    gzip on;
    gzip_proxied any;
    gzip_types text/javascript application/x-javascript text/css;
    gzip_disable "msie6";
    gzip_comp_level 1;

<% if (config.static.minified) { %>

    location / {
        root "<%= root %>/webroot/static";
    }

<% } else { %>

    location / {
        root "<%= root %>/lib/client";
    }

    location /shared/ {
        root "<%= root %>/lib";
    }

<% } %>
}
